# India Cricket Dashboard — Environment Variables
# Copy this file to .env and fill in your values

# Server
PORT=3000

# Cron schedule (default: 7:00 AM IST = 01:30 UTC)
# Format: "minute hour * * *" in UTC
CRON_TIME=30 1 * * *

# Set to "true" to disable the cron scheduler (useful for standalone sync script)
DISABLE_CRON=false

# Anthropic API Key — REQUIRED for scraping (AI agent is the primary data source)
# The AI agent uses Claude to fetch and parse cricket schedules from bcci.tv and ESPN.
# Get yours at: https://console.anthropic.com/
# Add as GitHub secret: Settings → Secrets → Actions → ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Playwright browser path (optional — if using system Chromium)
# PLAYWRIGHT_CHROMIUM_PATH=/usr/bin/chromium-browser

# Node environment
NODE_ENV=production
